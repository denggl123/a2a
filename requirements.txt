fastapi==0.115.6
uvicorn==0.34.0
pytest==8.3.4
cryptography>=42.0.0
# starlette.testclient 需要 httpx：缺了它 `pytest tests` 会在**收集阶段**就
# RuntimeError 退出（"requires the httpx package"），新人按 README 走第一步就卡住。
httpx>=0.27
