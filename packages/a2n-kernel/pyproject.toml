[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "a2n-kernel"
version = "0.1.0"
description = "A2N 内核：hash 链、领域事件、策略版本化（零业务零依赖）"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
