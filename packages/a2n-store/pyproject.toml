[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "a2n-store"
version = "0.1.0"
description = "A2N 存储：schema、连接与 append-only 触发器"
requires-python = ">=3.11"
dependencies = ["a2n-kernel"]

[tool.setuptools.packages.find]
where = ["src"]
