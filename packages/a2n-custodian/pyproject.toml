[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "a2n-custodian"
version = "0.1.0"
description = "A2N 持牌清算方适配器：唯一能碰钱的包"
requires-python = ">=3.11"
dependencies = ["a2n-kernel", "a2n-store"]

[tool.setuptools.packages.find]
where = ["src"]
