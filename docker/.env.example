# 节点容器的本地配置模板（`docker/compose.yaml` 读同目录的 `.env`）。
#
# 用法：cp docker/.env.example docker/.env  然后填入真实值。`.env` **不入库**。
#
# A2N_STORAGE_KEY
#   32 字节随机数的 base64（恰好 44 字符，结尾一个 '='）。
#   Linux 上 `a2n_node.protection.system_protector()` 不许凭据明文落盘，
#   节点库 `runtime.db` 就是用它封的 —— **一旦换了，旧卷就读不出来**，
#   症状是启动时 `cryptography.exceptions.InvalidTag`，然后节点起不来。
#   生成： python -c "import os,base64;print(base64.b64encode(os.urandom(32)).decode())"
#   ⚠ 已有节点的机器**必须沿用原值**，否则等于换了一个身份、多出一批新节点。
A2N_STORAGE_KEY=
