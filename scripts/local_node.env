# 本机节点（宿主机上的那"1 本机"）的配置 —— 只有端口与档位，**没有密钥**。
#
# 为什么不含 A2N_STORAGE_KEY：这台是 Windows，`a2n_node.protection.system_protector()`
# 会走 `WindowsProtector`（DPAPI）—— 凭据由操作系统保护，不需要注入口令。
# （只有容器/ Linux 才必须注入：那里没有 DPAPI，宁可起不来也不许明文落盘。）
#
# 这个文件不含秘密，可以入库。路径不写在这里：`serve_local_node.sh` 自己按脚本位置
# 推仓库根，换机器不用改。
#
# 摆不摆摊：`A2N_CONTAINER` 留空 = **本节点不上架任何供给**，只做"这台电脑自己的节点"
# （身份 + 控制台 + P2P + 公益开关）。这是刻意的：旧演示里本机那四档
# （收费/免费/x402/试用中）依赖**平台**的免费期与毕业机制才能算得出"到期没有"，
# 平台已按用户拍板去掉，所以那四档在无平台形态下**没有意义**。
# 想让它也摆摊：填 video-studio / finance-legal / play-ecom 之一
# （那三组案例已经在三个容器里各挂一份，本机再挂就等于同一份供给出现两次）。

# 对外声明的入口（= 本机反代监听的地址）。回环用明文 HTTP 是允许的
# （PublicDirectoryClient.normalize_base 只对"不离开本机"的地址放行明文）。
A2N_PUBLIC_BASE=http://127.0.0.1:8891
# 反代监听端口（公开地址的端口，非 80/443 时两边一致）
A2N_PUBLIC_PORT=8891
# 节点本体（只绑回环）：控制台与 A2A 入口
A2N_PORT=8890
# P2P 发现 UDP 端口
A2N_P2P_PORT=9788
# 空 = 不摆摊
A2N_CONTAINER=
# 公益开关：自愿把本节点的目录开放给别的节点当发现来源
A2N_PUBLIC_SERVICE=1
